/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "discoverdobrogea.ro",
            "commons.wikimedia.org",
            "images.unsplash.com",
            "cdn.adh.reperio.news"
        ]
    }
}

module.exports = nextConfig
